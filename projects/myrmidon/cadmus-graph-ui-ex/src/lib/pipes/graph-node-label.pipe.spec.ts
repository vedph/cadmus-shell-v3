import { GraphNodeLabelPipe } from './graph-node-label.pipe';

describe('GraphNodeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new GraphNodeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
