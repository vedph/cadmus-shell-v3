import { HasPreviewPipe } from './has-preview.pipe';

describe('HasPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new HasPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
